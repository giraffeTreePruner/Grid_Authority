/**
 * Entry point for the read-only JSON API.
 *
 * The server is assembled in later tasks; this module exists so the package
 * builds and type-checks from the start.
 */
export const API_VERSION = 'v1' as const;
